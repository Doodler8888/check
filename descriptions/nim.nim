- Compile a code:
  nim c -d:release myprogram.nim




The -d:release flag tells Nim to compile in release mode, which applies optimizations 
and omits debugging information. This makes the resulting binary faster and smaller.
